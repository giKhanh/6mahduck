{% form_theme storeForm 'bootstrap_5_layout.html.twig' %}
{% extends "base.html.twig" %}

{% block body %}
<div style="background-color: lightbule"> 
<div class="container col-md-4 text-center mt-3">
   <div class="card p-3 bg-light">
      <h3 class="alert alert-primary">Edit store</h3>
      {# TH1: Nếu đã add nút Submit ở trong form #}
      {# {{ form (storeForm )}} #}

      {# TH2: Nếu chưa add nút Submit ở trong form #}
      {{ form_start(storeForm) }}
         
      {{ form_widget(storeForm)}}
      <input class="btn btn-outline-danger" type="submit" value="Edit">

      {{ form_end(storeForm) }}
   </div>
</div>
</div>
{% endblock %}